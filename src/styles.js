const Button = {
    background: "none",
    border: "1px solid",
    borderRadius: 2,
    color: "black",
    padding: "6px 12px",
    textAlign: "center",
    textDecoration: "none",
    display: "inline-block",
    fontWeight: "bold",
    fontFamily: "Helvetica",
    fontSize: "12px"
}

const ButtonPress = {
    background: "none",
    border: "1px solid",
    borderRadius: 2,
    color: "#5f6a72",
    padding: "6px 12px",
    textAlign: "center",
    textDecoration: "none",
    display: "inline-block",
    fontWeight: "bold",
    fontFamily: "Helvetica",
    fontSize: "12px"
}

const ConStyles = {
    fontFamily: "Helvetica"
}

const styles = {
    Button: Button,
    ButtonPress: ButtonPress,
    ConStyles: ConStyles
}

export default styles